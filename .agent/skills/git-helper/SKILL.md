---
name: git-helper
description: Guide safe commit/PR/release workflows. Auto-trigger keywords: commit, branch, PR, changelog, review, release.
---

# Git Helper

## Single Workflow

1. Summarize intended git outcome.
2. Propose minimal command sequence.
3. Require approval for risky commands.
4. Provide rollback hint when possible.

## Safety

- `git push --force` requires explicit rejection (do not run).
