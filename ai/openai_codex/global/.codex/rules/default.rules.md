<!-- Codex Rules 正本は同ディレクトリの `default.rules`（拡張子 `.rules`）。 -->
<!-- sync で `~/.codex/rules/` へコピーされる。編集は `default.rules` を直す。 -->

正本: [`default.rules`](./default.rules)

Codex は `*.rules`（Starlark）を読む。この `.md` は案内のみ。
